name: Publish to BCR

on:
  release:
    types: [published]

permissions:
  id-token: write
  attestations: write
  contents: write

jobs:
  publish:
    uses: bazel-contrib/publish-to-bcr/.github/workflows/publish.yaml@main
    with:
      tag_name: ${{ github.event.release.tag_name }}
      registry_fork: nalinraut/bazel-central-registry
      attest: false
    secrets:
      publish_token: ${{ secrets.BCR_PUBLISH_TOKEN }}
